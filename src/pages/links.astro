---
import type { LinksEntry } from "@/types";
import EntryLayout from "@components/links/EntryLayout.astro";
import { getIndex } from "@lib/contentParser";

const entry = (await getIndex("links")) as LinksEntry;
---

<EntryLayout entry={entry} />
