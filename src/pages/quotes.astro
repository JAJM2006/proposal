---
import { getIndex } from "@lib/contentParser";
import EntryLayout from "@components/quotes/EntryLayout.astro";
import type { QuotesEntry } from "@/types";

const entry = (await getIndex("quotes")) as QuotesEntry;
---

<EntryLayout entry={entry}/>
